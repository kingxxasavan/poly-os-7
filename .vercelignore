# Vercel gets only the website: the pages in docs/ and the Poly Account API (api/ and server/).
# Everything else here is PolyOS itself, which Vercel doesn't need.
/*
!/docs
!/api
!/server
!/package.json
!/package-lock.json
!/vercel.json
/server/tests
/server/dev.js
