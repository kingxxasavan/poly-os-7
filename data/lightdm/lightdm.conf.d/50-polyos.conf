# Installed by polyos-desktop: PolyOS is the default session and the PolyOS login screen is
# the greeter (it falls back to lightdm-gtk-greeter on its own if it can't start).
[Seat:*]
user-session=polyos
greeter-session=polyos-greeter
